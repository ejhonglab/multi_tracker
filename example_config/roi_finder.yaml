roi_finder/roi_type: 'polygon'
roi_finder/automatic_roi_detection: False
roi_finder/toss_first_n_frames: 0
# TODO TODO dilate polygons (to not lose border for misclicking when defining ROI?)
# just convert to mask and then dilate? (seems like there would be a cheaper way though)
#roi_finder/extra_roi_pixel_radius: 5
